apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-microservice-chart.fullname" . }}
  labels:
    {{- include "go-microservice-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "go-microservice-chart.selectorLabels" . | nindent 4 }}
